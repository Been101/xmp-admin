language: node_js
node_js:
  - "14"
install: npm install
after_script:
  - cd ./public
  - git init
  - git config user.name "been"
  - git config user.email "1043609130@qq.com"
  - git add -A
  - git commit -m "updated"
  -git push --force "https://{GH_TOKEN}@git@${GH_REF}" "main:gh_pages"

